pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // allow both, prefer settings-level
    repositories {
        mavenCentral()
    }
}

rootProject.name = "beyond-medicine-assignment-kotlin"
